{
    "env": {
        "JWT_KEY": "This is the secret no one must be know",
        "port": 8080,
        "mongo_url": "mongodb://localhost:27017/ghost",
        "api_url": "http://localhost:8080/api",
        "DB_ENV": "development"
    }
}